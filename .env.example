# Copy this file to .env and fill in real values for local `netlify dev` testing.
# Never commit the real .env file (it's git-ignored).

# Get a key from https://console.anthropic.com
ANTHROPIC_API_KEY=

# A passphrase only you know — required to use the /tailor.html page locally and in production.
TAILOR_PASSPHRASE=
